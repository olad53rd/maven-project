pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'building the codes'
            }
        }
 pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                sh 'echo testing the codes'
             }
        }
